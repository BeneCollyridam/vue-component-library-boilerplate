import CCheckbox from './c-checkbox.vue';

describe('@/components/c-checkbox.vue', () => {
  it('should export with name: "c-checkbox"', () => {
    expect(CCheckbox.name).toBe('c-checkbox');
  });
});
