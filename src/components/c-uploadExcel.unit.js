import CUploadExcel from './c-uploadExcel.vue';

describe('@/components/c-uploadExcel.vue', () => {
  it('should export with name: "c-uploadExcel"', () => {
    expect(CUploadExcel.name).toBe('c-uploadExcel');
  });
});
