import CSnackbar from './c-snackbar.vue';

describe('@/components/c-snackbar.vue', () => {
  it('should export with name: "c-snackbar"', () => {
    expect(CSnackbar.name).toBe('c-snackbar');
  });
});
