import LDashboard from './l-dashboard.vue';

describe('@/components/l-dashboard.vue', () => {
  it('should export with name: "l-dashboard"', () => {
    expect(LDashboard.name).toBe('l-dashboard');
  });
});
