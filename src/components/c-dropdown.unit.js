import CDropdown from './c-dropdown.vue';

describe('@/components/c-dropdown.vue', () => {
  it('should export with name: "c-dropdown"', () => {
    expect(CDropdown.name).toBe('c-dropdown');
  });
});
